# Optional. Typical users paste the key in the editor on first Chat use
# (Config → NVIDIA NIM API key…). Never commit `.env`.
#
# Lookup order: process env → stored user://ai_secrets.cfg → .env beside
# the binary / cwd / user://.env / res://.env (editor only).

NVIDIA_NIM_API_KEY=
